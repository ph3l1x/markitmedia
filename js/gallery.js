jQuery(document).ready(function($) {
	 $('#gallery').galleryView({
		panel_width: 970,
		panel_height: 450,
		frame_width: 120,
		frame_height: 90,
		show_overlays: true,
		show_filmstrip_nav: false
	});
});